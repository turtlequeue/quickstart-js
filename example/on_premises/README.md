```
const q = aTurtleParent.make({
  host: 'turtlequeue.com',     // replace with your company's host
  type: 'ws',                  // 'ws' for "websocket"
  protocol: 'https',           // can be 'http' if used behind your company's firewall
})
```
